services:
  postgres:
    image: postgres:15
    container_name: agenda_postgres
    restart: always
    environment:
      POSTGRES_DB: bd_apiagenda
      POSTGRES_USER: postgresuser
      POSTGRES_PASSWORD: postgrespassword
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: